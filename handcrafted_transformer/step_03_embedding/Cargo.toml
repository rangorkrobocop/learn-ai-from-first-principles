[package]
name = "step_03_embedding"
version = "0.1.0"
edition = "2021"
description = "Step 3: Words as GPS Coordinates on a Map (Embeddings)"

[dependencies]
burn = { version = "0.21", features = ["ndarray"] }
