[package]
name = "step_05_layer_mlp"
version = "0.1.0"
edition = "2021"
description = "Step 5: Stacking Cells Into a Brain (Layers & the MLP)"

[dependencies]
rand = "0.8"

[profile.release]
opt-level = 3
